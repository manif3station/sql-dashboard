# 2026-04-29 documentation refresh

## Summary

Rewrote the `sql-dashboard` skill docs so they describe the SQL workspace by user-facing function instead of by extraction origin.

## What changed

- refreshed `README.md` to explain the SQL workspace, what problem it solves, and how users work with it
- refreshed the overview and usage docs with practical SQL workflow language
- updated install examples to use the skill repo remote used in this workspace
- kept the database support notes focused on runtime capability and setup

## Why

The skill docs needed to read like a real skill manual for users landing on `sql-dashboard`, not like internal packaging notes.
