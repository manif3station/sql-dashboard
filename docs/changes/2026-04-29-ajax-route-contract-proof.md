# 2026-04-29 Ajax Route Contract Proof

- rechecked the DD 3.24 ajax router and proved it still resolves the flat `sql-dashboard-*` ajax file contract
- kept the extracted page and ajax worker filenames aligned with the current runtime route shape
- updated tests to prove the route contract and worker-body matching instead of only checking an exact copied page hash
