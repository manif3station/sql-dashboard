# EPIC-026

## Title

Replace `sql-dashboard` screenshots with real SQLite demo views.

## Goal

Regenerate the `sql-dashboard` README screenshots from a working SQLite demo dataset inside Docker so the docs show the skill in a functional state.
